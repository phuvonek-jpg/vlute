{
  "version": 2,
  "builds": [
    {
      "src": "server.js",
      "use": "@vercel/node",
      "config": { "includeFiles": ["index.html", "assets/**"] }
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "server.js" }]
}
